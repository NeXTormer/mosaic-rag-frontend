import 'package:flutter/material.dart';

class ConversationalSearchSection extends StatelessWidget {
  const ConversationalSearchSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder(
      child: Center(child: Text("Coming soon")),
    );
  }
}
